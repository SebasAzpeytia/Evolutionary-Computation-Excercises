"""def generatePotentialPopulationByMatingPool():
matingPool = []
for character in self.population:
    quantity = character.fitness * 100
    for _ in range(int(quantity)):
        matingPool.append(character)
self.population = matingPool[0 : len(self.population)]"""
